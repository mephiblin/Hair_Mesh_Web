{
  "name": "hair-mesh-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node tests/core-tests.mjs",
    "check": "npm test && node --check src/**/*.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
