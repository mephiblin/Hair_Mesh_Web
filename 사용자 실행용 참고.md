# Hair Mesh Web 실행 참고

## Linux

터미널에서 다음 명령을 실행합니다.

```bash
cd /home/inri/문서/Hair_Mesh_Web
python3 launch_server.py
```

브라우저가 자동으로 열리지 않으면 터미널에 표시되는 주소를 복사해 여십시오.

## Windows

프로젝트 폴더의 `3D_Web_Paint_실행.cmd`를 더블클릭합니다.

`.cmd`가 메모장으로 열리는 PC에서는 명령 프롬프트를 열고 프로젝트 경로로 이동한 뒤 직접 실행합니다.

```bat
cd /d C:\path\to\Hair_Mesh_Web
3D_Web_Paint_실행.cmd
```

또는 Python이 설치되어 있다면 같은 위치에서 `python launch_server.py`를 실행합니다.

## 주의

- `curve_mesh_hair_tool_v4.html`을 직접 더블클릭하면 ES Module/CORS 제한으로 정상 실행되지 않습니다.
- Three.js를 CDN에서 불러오므로 인터넷 연결이 필요합니다.
- 브라우저 주소 형식은 `http://127.0.0.1:<포트>/curve_mesh_hair_tool_v4.html`입니다.
- 개발 검사까지 실행하려면 `npm install` 후 `npm run check`를 사용합니다. Viewport pointer/gizmo 회귀까지 확인하려면 Playwright Chromium을 설치한 뒤 `npm run test:viewport`를 실행합니다.
